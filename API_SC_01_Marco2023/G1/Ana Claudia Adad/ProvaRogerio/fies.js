import { question } from "readline-sync";

function main(){
const duração_curso = Number(question('Duração do curso: '))
const mensalidade = Number(question('Valor da mensaldade: '))
const taxa_selic = Number(question('Taxa selic: '))
const salario_minimo = Number(question('Valor Salário mínimo: '))
const renda_familia = Number(question('Renda familiar em (R$): '))
const pessoas_na_familia = Number(question('Quantas pessoas tem na familia: '))
const ano = Number(question('O ano: '))
const semestre_inicio = Number(question('Inicio do ano e semestre: '))

function renda_familia(descricao){
    if{
        (renda_familia < 1,5)
        return taxa_selic == 10%

    }else{
        (renda_familia >= 2)
        return taxa_selic ==15%

    }if else{
        (renda_familia > 2 || 2,5)
        return taxa_selic == 20%
    }if else{
        (renda_familia > 2,5)
        return taxa_selic ==25%
    }
    }
    
console.log('')
}

}


   

main()