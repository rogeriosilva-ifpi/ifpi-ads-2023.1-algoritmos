//Pseudocódigo:

/* 1º Pedir letras de duas figuras em apenas um input;
        letras_validas = ['Q', 'q', 'R', 'r', 'T', 't', 'Z', 'z','C','c' ]
        Se letra for != (letras_validas){
            console.log('Letra não é válida!')
        }
        pede um novo input;

   2º Verificar qual é a figura em cada posição e pedir os dados de cada uma;
   if (figuras[0]):
        se figuras[0] for igual a (Q ou q):
            pedir um input para lado
        se figuras[0] for igual a (R ou r):
            pedir um input para comprimento e largura (separar com espaço e fazer o split)
        se figuras[0] for igual a (T ou t):
            pedir um input para base e altura ()


            NÃO TERMINEI, PORQUE PASSEI PARA AS OUTRAS.
*/


