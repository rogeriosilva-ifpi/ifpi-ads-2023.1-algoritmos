import { question } from "readline-sync";
function main(){

    const nome_completo = question(" > informe o seu nome completo : ")

    const idade = Number(question("> informe a sua idade : "))

    const mediasemestraldoaluno = Number(question(" > informe a sua media semestral :"))
}

main()
