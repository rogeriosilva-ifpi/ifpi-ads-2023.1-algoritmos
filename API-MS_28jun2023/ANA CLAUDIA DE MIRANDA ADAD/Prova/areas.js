import { question } from "readline-sync";


function main(){

    const figuras = question("Escolha duas figura:")


    let count = () => `${figuras.length} figuras`

    clear_screen()

    let menu = () => `#### Figuras [${count()}] ####
    Q/q: Quadrado
    R/r: Retangulo
    T/t: Triangulo
    Z/z: Trapezio
    C/c: Circulo
    \n >>> `

    while(opcao === quadrado){

    }
}
